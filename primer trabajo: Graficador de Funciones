//graficador de funcines en el lenguaje de python//
import numpy as np
import matplotlib.pyplot as plt

# Definir la función
def f(x):
    return x ** 2

# Rango de valores para la variable independiente x
x = np.linspace(-5, 5, 400)

# Evaluar la función en los valores de x
y = f(x)

# Crear la gráfica
plt.plot(x, y, label='$f(x) = x^2$')

# Añadir etiquetas a los ejes
plt.xlabel('$x$')
plt.ylabel('$f(x)$')

# Añadir título al gráfico
plt.title('Gráfico de $f(x) = x^2$ en el intervalo [-5, 5]')

# Mostrar leyenda
plt.legend()

# Mostrar la gráfica
plt.grid(True)
plt.show()
